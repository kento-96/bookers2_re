<% if book.favorited_by?(current_user)%>
 <%= link_to book_favorites_path(book),method: :delete,local:true do %>
   ♥<%= book.favorites.count %> いいね
    <% end %>
  <% else %>
    <%= link_to book_favorites_path(book),method: :post,local: true do %>
      ♡<%= book.favorites.count %> いいね
    <% end %>
  <% end %>